import { useQuery } from "@tanstack/react-query";
import { QueryKeys } from "../client";
import { IPart } from "@/src/type/part.interface";

// export const useGetParts = () => {
//   const result = useQuery<IPart[]>([QueryKeys.PART], getParts);

//   return result;
// };
